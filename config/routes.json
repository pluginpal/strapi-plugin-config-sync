{
  "routes": [
    {
      "method": "GET",
      "path": "/export",
      "handler": "config.export",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/import",
      "handler": "config.import",
      "config": {
        "policies": []
      }
    }
  ]
}
